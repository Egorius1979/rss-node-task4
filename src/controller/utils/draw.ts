import { ILoc } from "../controller";

export const draw = (loc: ILoc, data: string[]) => {
  // const shape = data[0];
  // const step: number = +data[1];
  // let x: number = loc.x,
  //   y: number = loc.y;
  // if (shape === "square") {
  //   y = loc.y - step;
  // } else if (shape === "down") {
  //   y = loc.y + step;
  // } else if (shape === "left") {
  //   x = loc.x - step;
  // } else if (shape === "right") {
  //   x = loc.x + step;
  // }
};
